<h1> Hello Rails</h1>
<%= link_to 'My Blog', controller: 'articles' %>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= article.title %><br />
      <%= article.content %>
    </li>
  <% end %>
</ul>

<a href="/articles/new">New Article</a>